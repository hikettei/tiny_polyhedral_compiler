from __future__ import annotations

from dataclasses import dataclass, field
import caten.ir as ir
# [TODO] Whether They should be defined as


# [TODO] ⇩ CatenOpにするべきだろうか？

# Exec is VMAP
      # reads        # writes
# Exec(UnionMap(...), UnionMap(...), op)
